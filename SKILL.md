---
name: taobao-auto-listing
description: "淘宝/天猫店铺商品自动化上架全流程助手。支持「登录后全自动执行模式」：用户扫码登录千牛后，从类目选择、标题属性填写、主图详情图上传、SKU价格库存设置、物流售后到发布上架及核验，全部连续自动完成，最后输出上架结果汇总。覆盖商品资料准备、批量上架、上架后核验。当用户需要上架新品、批量上架、修改商品信息、或询问淘宝卖家中心/千牛工作台发布商品操作时使用此 Skill。适用于文具、服饰、数码、食品等各类目商品。"
---

# 淘宝店铺商品自动化上架

## 概述

本 Skill 指导通过浏览器自动化在淘宝卖家中心（千牛工作台 PC 端）完成商品从准备到发布的全流程上架。核心执行路径为**浏览器操作**，配合结构化商品资料模板和校验清单，确保上架信息完整、合规、可搜索。

默认采用**全自动执行模式**：用户扫码登录后，所有步骤连续自动完成，中途不暂停不询问，最终输出上架结果汇总。

## 执行前必读

1. **登录态**：首次执行需确认浏览器已登录淘宝卖家账号。若未登录，引导用户完成扫码登录后再继续。
2. **商品资料**：上架前必须收集完整的商品信息。使用 `assets/product-info-template.csv` 模板逐项填写，或按 `references/listing-checklist.md` 清单逐项确认。
3. **类目预判**：根据商品名称和属性，提前预判正确类目。类目选择错误会导致流量不准甚至违规下架。参考 `references/category-attributes.md`。
4. **图片合规**：主图和详情图必须符合淘宝规范（尺寸、格式、文字占比）。参考 `references/image-specs.md`。

## 全自动执行模式（默认）

用户扫码登录千牛后，**连续执行所有上架步骤，中途不暂停、不询问**，直到发布完成或遇到必须人工处理的异常。

### 仅以下情况需要暂停并通知用户

1. **登录失效** — 页面跳转到登录页
2. **验证码/滑块验证** — 出现人机验证
3. **资质缺失** — 所选类目需要店铺未具备的资质
4. **资料缺失** — 商品资料中有必填项为空（应在执行前已校验，理论上不出现）
5. **页面严重异常** — 连续3次操作失败或页面结构完全无法识别

除此之外，所有步骤（类目选择、信息填写、图片上传、价格库存、物流售后、发布、核验）均自动连续完成。每完成一个阶段内部记录进度，不输出中间过程给用户，全部完成后统一输出「上架结果汇总」。

### 执行节奏

1. 打开卖家中心 → 检测登录态
   - 未登录：切换扫码登录，通知用户扫码，等待登录完成
   - 已登录：直接进入下一步
2. 登录完成后，**连续执行阶段三到阶段七**，每步之间等待页面加载即可，不向用户确认
3. 发布成功后自动执行上架核验
4. 输出「上架结果汇总」

### 批量上架全自动模式

批量上架时，逐个商品连续执行，每个商品完成后记录结果，不中断。全部完成后输出汇总表。单个商品失败不影响后续商品，记录失败原因后继续下一个。

## 上架全流程

### 阶段一：商品资料准备

在操作浏览器之前，先确认以下资料已齐备：

| 资料项 | 说明 | 必填 |
|--------|------|------|
| 商品标题 | 30字以内，含核心关键词 | 是 |
| 商品类目 | 精确到三级/四级类目 | 是 |
| 商品属性 | 品牌、材质、规格等类目属性 | 是 |
| 主图 | 至少1张，最多5张（部分类目支持更多） | 是 |
| 详情图/详情页 | 商品描述图片或文字 | 是 |
| SKU规格 | 颜色、尺寸等规格及对应价格库存 | 视类目 |
| 价格 | 一口价/各SKU价格 | 是 |
| 库存 | 总库存或各SKU库存 | 是 |
| 运费模板 | 已创建好的运费模板名称 | 是 |
| 售后服务 | 发票、保修、退换货设置 | 是 |

> 资料不完整时，先向用户索取缺失项，不要在发布页面中途停顿等待。

### 阶段二：登录与导航

1. 打开浏览器，访问 `https://myseller.taobao.com/`（淘宝卖家中心）。
2. 确认已登录卖家账号（页面右上角显示店铺名称）。
3. 左侧导航栏找到 **「商品」→「发布商品」**，点击进入发布页面。
   - 新版卖家中心路径：「商品」→「商品管理」→「发布商品」
   - 旧版路径：「我要卖」→「发布宝贝」

### 阶段三：类目选择

1. 在类目搜索框输入商品核心关键词（如「中性笔」「连衣裙」）。
2. 从搜索结果中选择最匹配的类目，精确到最后一级。
3. 点击「确认，去发布商品」进入信息填写页。

**注意事项**：
- 不确定类目时，搜索同类目热销商品，参考其类目归属。
- 部分类目需要资质（如食品需食品经营许可证），提前确认店铺资质。
- 类目一旦发布后修改可能导致降权，务必一次选对。

### 阶段四：填写商品基本信息

#### 4.1 商品标题
- 长度控制在 **25-30个汉字**（60字符以内）。
- 结构建议：**核心词 + 属性词 + 营销词 + 长尾词**。
- 标题撰写技巧参考 `references/title-optimization.md`。
- 禁止使用极限词（最、第一、国家级等）、违禁词。

#### 4.2 商品属性
- 根据所选类目，逐项填写带红色星号（*）的必填属性。
- 非必填属性尽量填写完整，属性越完整搜索匹配度越高。
- 品牌属性：无品牌选择「other/其他」，有品牌需确保品牌已入驻。
- 填写完成后检查是否有遗漏的必填项（页面会有红色提示）。

#### 4.3 商品主图
- 点击主图上传区域，上传准备好的主图。
- 第一张主图最重要，决定点击率。
- 主图要求参考 `references/image-specs.md`。
- 部分类目支持主图视频，可上传 9-30 秒视频提升转化。

#### 4.4 商品详情
- 上传详情页图片或在编辑器中输入文字描述。
- 详情页结构建议：**卖点海报 → 产品参数 → 细节展示 → 使用场景 → 品牌/售后说明**。
- 详情图宽度统一为 750px，高度不限，单张不超过 3MB。

### 阶段五：SKU 规格与价格库存

#### 5.1 规格设置（如类目支持）
- 勾选需要的规格维度（颜色分类、尺码等）。
- 为每个规格值上传对应规格图片（如颜色图）。
- 规格值名称要清晰易懂（如「黑色0.5mm」而非「规格1」）。

#### 5.2 价格设置
- 填写一口价（所有SKU中的最高价或统一价）。
- 为每个SKU填写单独价格。
- 注意：SKU价格差异不宜过大，避免被判定为SKU作弊。

#### 5.3 库存设置
- 填写总库存或各SKU对应库存数量。
- 库存建议设置为实际可售数量，避免超卖。
- 可使用「按SKU设置库存」分别填写。

#### 5.4 商家编码（可选）
- 如有内部货号，填写商家编码便于订单管理和库存对接。

### 阶段六：物流与售后服务

#### 6.1 运费模板
- 选择已创建的运费模板。
- 如未创建，点击「新建运费模板」跳转创建：
  - 设置发货地址
  - 设置计价方式（按件数/重量/体积）
  - 设置首件/首重运费和续件/续重运费
  - 设置指定地区运费（如偏远地区加价）
  - 设置包邮条件（如满多少包邮）

#### 6.2 发货时间
- 选择发货时间承诺（如24小时内、48小时内、72小时内）。
- 根据实际发货能力选择，超时发货会被处罚。

#### 6.3 售后服务
- **发票**：选择是否提供发票（企业店铺建议提供）。
- **保修**：根据商品类型选择保修期限。
- **退换货**：默认支持7天无理由退换货（部分类目除外）。
- **运费险**：建议开通，提升转化率。

### 阶段七：发布与核验

1. 点击页面底部「发布」按钮。
2. 如出现校验错误提示，根据提示返回修改对应字段。
3. 发布成功后，记录商品ID和商品链接。
4. **上架后核验**：
   - 打开商品详情页，确认标题、主图、价格、SKU显示正常。
   - 检查详情页图片是否完整加载。
   - 确认商品状态为「出售中」（非「仓库中」或「审核中」）。
   - 在卖家中心「出售中的宝贝」中搜索确认商品已上架。

## 批量上架

当用户需要批量上架多个商品时：

1. 为每个商品按 `assets/product-info-template.csv` 填写资料，汇总为一个CSV文件。
2. 逐个商品执行上述上架流程，每完成一个记录结果（商品ID + 链接 + 状态）。
3. 全部完成后输出上架结果汇总表。
4. 如商品数量超过10个，建议分批操作，每批5-10个，避免操作疲劳导致错误。

> 淘宝官方也提供「淘宝助理」或「商品发布API」用于批量上架，但需要额外授权和技术对接。浏览器自动化是最通用的方式。

## 常见问题与排查

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 发布按钮灰色不可点 | 有必填项未填写 | 检查页面红色提示，补全所有必填字段 |
| 提示「标题包含违禁词」 | 标题含极限词/敏感词 | 替换违禁词，参考广告法禁用词表 |
| 提示「类目属性不匹配」 | 所选类目与商品实际不符 | 重新选择正确类目 |
| 主图上传失败 | 图片尺寸/格式/大小不符合要求 | 按 `references/image-specs.md` 调整图片 |
| 发布后商品在仓库中 | 选择了「放入仓库」而非「立即上架」 | 到仓库中手动上架，或重新发布时选择立即上架 |
| SKU价格提示异常 | SKU价差过大或一口价设置错误 | 调整一口价为SKU最高价，缩小SKU价差 |
| 页面元素找不到 | 淘宝页面改版或加载未完成 | 等待页面完全加载，或刷新后重试 |

## 浏览器操作规范（全自动模式）

1. 每次页面跳转后等待页面完全加载（3-5秒）再进行下一步操作，不向用户确认。
2. 操作前截图确认当前页面状态（内部使用，不输出给用户）。
3. 遇到弹窗（提示、广告、通知）自动关闭后继续，不暂停。
4. 输入文字后截图验证输入框内容正确，不正确则重新输入。
5. 上传图片时使用本地文件绝对路径，确保文件存在且格式正确。
6. 操作过程中如遇登录失效或验证码，立即暂停并通知用户。
7. 全自动模式下不输出中间步骤进度，只在最终完成或需要人工介入时输出。
8. 每个阶段完成后内部记录状态，失败时记录失败点和原因。
9. 页面元素找不到时，先滚动页面查找，再刷新重试，最多2次。
10. 连续操作间隔不少于2秒，避免被判定为机器人行为。

## 异常处理与自动恢复

执行过程中遇到异常时，按以下优先级自动处理，只有无法自动恢复时才暂停通知用户：

| 异常 | 自动处理方式 | 是否需要人工 |
|------|-------------|-------------|
| 页面加载慢 | 等待5秒后重试，最多3次 | 否 |
| 元素找不到 | 刷新页面后重试，最多2次 | 否（2次失败后通知） |
| 弹窗/广告 | 自动关闭弹窗后继续 | 否 |
| 图片上传失败 | 检查文件路径，重试1次 | 否（仍失败则记录并继续） |
| 标题违禁词提示 | 自动替换同义词后重试 | 否 |
| 必填项遗漏 | 自动返回补填（资料应已齐全） | 否 |
| 验证码/滑块 | 暂停，通知用户手动完成 | 是 |
| 登录失效 | 暂停，通知用户重新登录 | 是 |
| 资质不足 | 暂停，通知用户补充资质或换类目 | 是 |
| 发布后商品在仓库 | 自动到仓库中点击上架 | 否 |

**自动恢复原则**：能重试的先重试，能自动修正的先修正，确实需要人工介入才暂停。暂停时明确告诉用户：卡在哪一步、需要做什么、完成后如何继续。

## 上架结果汇总（必输出）

上架完成后，必须按以下格式输出结果汇总。

### 单个商品成功

```
✅ 商品上架成功

商品名称：[标题]
商品ID：[淘宝商品ID]
商品链接：[详情页URL]
商品类目：[类目路径]
价格：[一口价] 元
库存：[数量] 件
SKU：[规格列表]
主图：[已上传张数]/5
详情图：[已上传张数]
上架时间：[时间]
商品状态：出售中 ✓
核验结果：标题/主图/价格/SKU/详情页 均正常
```

### 批量上架

```
📊 批量上架结果汇总

成功：[N] 件
失败：[M] 件
总耗时：[X] 分钟

成功商品：
| 序号 | 商品名称 | 商品ID | 价格 | 链接 |
|------|----------|--------|------|------|
| 1 | ... | ... | ... | ... |

失败商品：
| 序号 | 商品名称 | 失败原因 | 处理建议 |
|------|----------|----------|----------|
| 1 | ... | ... | ... |
```

### 失败时

```
❌ 商品上架失败

商品名称：[标题]
失败阶段：[类目选择/信息填写/图片上传/发布]
失败原因：[具体错误信息]
已完成步骤：[已完成的阶段列表]
建议操作：[用户需要做什么]
```

## 参考资源

- **上架校验清单**：`references/listing-checklist.md` — 发布前逐项核对
- **图片规范**：`references/image-specs.md` — 主图/详情图/视频的尺寸格式要求
- **标题优化**：`references/title-optimization.md` — 标题撰写技巧与关键词策略
- **类目与属性**：`references/category-attributes.md` — 常见类目属性填写指南
- **商品资料模板**：`assets/product-info-template.csv` — 结构化商品信息录入模板
